import 'package:flutter/material.dart';

abstract final class AppColors {
  // Brand
  static const Color primary = Color(0xFF6C4AB6);
  static const Color primaryDark = Color(0xFF533594);
  static const Color primaryLight = Color(0xFFF5F1FF);

  // Period
  static const Color period = Color(0xFFE84D6E);
  static const Color periodBackground = Color(0xFFFFF3F5);

  // Cycle
  static const Color cycle = Color(0xFF8E6AD8);
  static const Color cycleBackground = Color(0xFFF5F1FF);

  // Neutral
  static const Color background = Color(0xFFFBF9FD);
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF332F3A);
  static const Color textSecondary = Color(0xFF8D8793);
  static const Color border = Color(0xFFE7E0EC);
}